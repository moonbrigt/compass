---
date: 2025-08-26
tags:
  - daily
  - example
dq_goals: 7
dq_progress: 8
dq_meaning: 6
dq_happy: 8
dq_relationships: 5
dq_engaged: 9
habit_journal: true
habit_exercise: true
habit_reading: false
---
« [[Compass Dashboard|Compass 仪表盘]] »（一年前的示例笔记；模板中没有对应的周笔记和季度笔记，因此省略常规导航链接。）

# 2025年8月26日 星期二

> [!theme]- 生活主题
> ![[Life Theme#主题]]

## 今日
- [x] 当天已完成的示例任务 ✅ 2025-08-26

## 日记
- 21:10 这是一年前的示例记录。打开今天的笔记（[[2026-08-26]]），它会出现在**往年今日**下。仓库日记的意义之一，就是在意想不到的时候遇见过去的自己。

## 收获
- 午饭前完成了通讯的第一版草稿。

## 感恩
- 感冒恢复期间慢跑了 3 英里。我给努力程度打了 9 分，并不是因为跑得好。

## 每日问题
已通过提示词回答（见上方属性）。

## 往年今日
```dataviewjs
const me = dv.current().file.name;
const cfg = dv.page("元数据/Compass Config") || {};
const folder = cfg.daily_folder || "01 日记/每日";
if (/^\d{4}-\d{2}-\d{2}$/.test(me)) {
  const mmdd = me.slice(4);
  const yr = parseInt(me.slice(0, 4));
  const hits = dv.pages(`"${folder}"`).where(p => p.file.name !== me && p.file.name.endsWith(mmdd)).sort(p => p.file.name, "desc");
  if (hits.length === 0) dv.paragraph("*往年还没有同一天的记录。可以留下一条未来的自己会用得上的线索。*");
  for (const p of hits) {
    const n = yr - parseInt(p.file.name.slice(0, 4));
    dv.header(4, `${n} 年前：${p.file.link}`);
    dv.paragraph(`![[${p.file.name}#日记]]`);
  }
}
```
