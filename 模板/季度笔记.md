---
quarter: <% tp.file.title %>
retreat: "[[02 静修/<% tp.file.title %> 个人静修]]"
focus_area: 
tags:
  - quarterly
---
« [[01 日记/季度/<% moment(tp.file.title, "YYYY-[Q]Q").subtract(1, "quarter").format("YYYY-[Q]Q") %>|上季度]] · [[Compass 总览|Compass 仪表盘]] · [[01 日记/季度/<% moment(tp.file.title, "YYYY-[Q]Q").add(1, "quarter").format("YYYY-[Q]Q") %>|下季度]] »

<% moment(tp.file.title, "YYYY-[Q]Q").startOf("quarter").locale("zh-cn").format("M月D日") %> 至 <% moment(tp.file.title, "YYYY-[Q]Q").endOf("quarter").locale("zh-cn").format("YYYY年M月D日") %> · 个人静修：[[02 静修/<% tp.file.title %> 个人静修|本季度个人静修]]

> [!theme]- 生活主题与核心价值观
> ![[人生主题#主题]]
> ![[核心价值观#价值观]]

```agent
type: button
text: "准备个人静修"
prompt: "请用 vault_read 阅读 提示词/04 静修准备.md，并针对当前打开的笔记执行其中的“## 提示词”部分；如果当前笔记不适用，则使用当前时间段。"
viewType: right-pane
```

## 季度意向
在个人静修时确定。将意向复制到这里，或嵌入静修笔记中的对应章节，供每周笔记引用。
![[<% tp.file.title %> 个人静修#5. 下季度意向]]

## 关注领域（来自生活之轮）
- 

## 本季度项目
```dataview
TABLE WITHOUT ID file.link AS 项目, status AS 状态, area AS 领域, due AS 到期日
FROM "04 项目"
WHERE quarter = "<% tp.file.title %>" AND status != "done"
SORT due ASC
```

## 每周笔记
```dataview
LIST
FROM "01 日记/每周"
WHERE quarter = "<% tp.file.title %>"
SORT file.name ASC
```

## 本季度每日问题
```dataviewjs
await dv.view("元数据/视图/dailyquestions", { from: "<% moment(tp.file.title, "YYYY-[Q]Q").startOf("quarter").format("YYYY-MM-DD") %>", to: "<% moment(tp.file.title, "YYYY-[Q]Q").endOf("quarter").format("YYYY-MM-DD") %>" });
```

## 季末记录
在下一次个人静修的回顾中参考这些记录。
- 
