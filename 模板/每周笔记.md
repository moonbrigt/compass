---
week: <% tp.file.title %>
quarter: <% moment(tp.file.title, "gggg-[W]ww").format("YYYY-[Q]Q") %>
tags:
  - weekly
---
« [[01 日记/每周/<% moment(tp.file.title, "gggg-[W]ww").subtract(1, "week").format("gggg-[W]ww") %>|上周]] · [[01 日记/季度/<% moment(tp.file.title, "gggg-[W]ww").format("YYYY-[Q]Q") %>|本季度]] · [[Compass 总览|Compass 仪表盘]] · [[01 日记/每周/<% moment(tp.file.title, "gggg-[W]ww").add(1, "week").format("gggg-[W]ww") %>|下周]] »

# <% moment(tp.file.title, "gggg-[W]ww").format("[第 ]w[ 周（]gggg[ 年）]") %>
<% moment(tp.file.title, "gggg-[W]ww").startOf("week").locale("zh-cn").format("M月D日") %> 至 <% moment(tp.file.title, "gggg-[W]ww").endOf("week").locale("zh-cn").format("M月D日") %>

日期：<%* const s = moment(tp.file.title, "gggg-[W]ww").startOf("week"); const parts = []; for (let i = 0; i < 7; i++) parts.push(`[[01 日记/每日/${s.clone().add(i, "day").format("YYYY-MM-DD")}|${s.clone().add(i, "day").locale("zh-cn").format("ddd")}]]`); tR += parts.join(" · "); %>

> [!intention]- 季度意向
> ![[01 日记/季度/<% moment(tp.file.title, "gggg-[W]ww").format("YYYY-[Q]Q") %>#季度意向]]

## 本周意向
本周完成后能推进季度意向的三件事。
1. 
2. 
3. 

## 检查理想一周
查看 [[理想一周|理想的一周]]。本周何时落实上述意向？现在就调整日历，不要等到周四。

- 

## 本周到期
```tasks
not done
due after <% moment(tp.file.title, "gggg-[W]ww").startOf("week").subtract(1, "day").format("YYYY-MM-DD") %>
due before <% moment(tp.file.title, "gggg-[W]ww").endOf("week").add(1, "day").format("YYYY-MM-DD") %>
sort by due
group by filename
```

```agent
type: button
text: "复盘本周"
prompt: "请用 vault_read 阅读 提示词/03 每周回顾.md，并针对当前打开的笔记执行其中的“## 提示词”部分；如果当前笔记不适用，则使用当前时间段。"
viewType: right-pane
```

## 每周复盘
周末填写。下方从日记中汇总每日努力程度评分和习惯完成情况。
```dataviewjs
await dv.view("元数据/视图/week", { week: dv.current().file.name });
```

### 做得好的事

### 不顺利的事

### 本周收获
```dataview
LIST L.text
FROM "01 日记/每日"
FLATTEN file.lists AS L
WHERE L.section.subpath = "收获" AND file.day >= date(<% moment(tp.file.title, "gggg-[W]ww").startOf("week").format("YYYY-MM-DD") %>) AND file.day <= date(<% moment(tp.file.title, "gggg-[W]ww").endOf("week").format("YYYY-MM-DD") %>)
SORT file.name ASC
```
